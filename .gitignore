node_modules/
dist/
.worktrees/
*.bak
*.tsbuildinfo
.DS_Store

# CodeGraph local index (SQLite + tree-sitter cache)
.codegraph/

# claude-mem session-memory dump — auto-regenerated each session, not source
AGENTS.md

# throwaway scratch/debug scripts
temp/
# throwaway scratch/debug scripts (kept out), except the versioned ARCH tracker
.scratch/*
!.scratch/arch-workstream
!.scratch/arch-workstream/**

# pnpm local store (package-manager cache, not source)
.pnpm-store/

# graft local graph cache — regenerable, not committed (run `graft build`)
/graft/

# secrets (audit CLI-M1) - never commit environment files
.env
.env.*
!.env.example
